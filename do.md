# 进入14阶段开发，另外修几个BUG或者新功能

## Chat相关

+ 新增：/Chat/{id} 页面时我希望搜索框内有个列表按钮，可以选择大模型（“根据setting配置获取的大模型”）
+ 新增：/Chat/{id} 页面进入对话框时自动滚动到对话框最下方
+ BUG：/Chat/{id} 明明配置好APIkey和URL发送消息仍然提示（回答没有生成完成回答生成失败：模型供应商鉴权失败，请检查 API Key 是否有效）。Setting配置完成后重新进入页面后的效果
+ BUG：/Chat/{id} 右侧引用来源可能无实际功能
+ BUG：/Chat/{id} 对话时我问他某个文档的标题让他总结，它似乎不知道内容。提高它的智能度

## Setting相关

+ Modify：/Settings "Chat Model"和"手动模型名称"合并既可以选择也可以手动填写
+ Add：/Settings 添加模型测试功能，用于测试模型是否真实可用
+ Delete：/Settings 删除文本描述“Modify：/Settings 添加模型测试功能，用于测试模型是否真实可用” 、“保存成功只表示配置已写入后端，不代表模型调用一定可用。进入 Chat 前请先获取模型列表，确认 Base URL、API Key 和模型权限可用；后续如启用测试连接，以测试通过为准。”、
+ Add：/Settings 退出登录弹出确认退出登录确认框

## Header

+ Uncertain：邮箱ICON角标不确定是否会有未读会话时显示对应的未读会话数角标，注意如果失败也要显示未读角标，两个状态分别是是否已读、是否成功。不管是否成功均看是否已读，未读则计入角标数字

## Common

+ 全局优化移动端及其不同分辨率的UI显示